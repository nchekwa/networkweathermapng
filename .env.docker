# Docker Environment Configuration
# Only main paths needed - other paths are built from these

# Application
APP_ENV=production
APP_DEBUG=false
APP_ROOT=/app
DATA_ROOT=/app/data

# Database
DB_TYPE=sqlite
DB_PATH=/app/data/weathermap.db
